{
  "name": "skill-wager-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Skill Wager Development Repository",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "start:venue": "node scripts/venue-daemon.js",
    "start:daemon": "node scripts/venue-daemon.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "ws": "^8.21.0"
  }
}
